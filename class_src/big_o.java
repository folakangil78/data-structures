void experiment(tbd) {
    int array_size = 2;

    while (array_size <= 4096){
        int[] test.array = array of size[array_size] write
                            random int values
    
        time_start = System.nanoTime();
        insertionSort(test.array)
        time_duration = System.nanoTime() - times.time_start;
        print(array-size).time.duration);

        test.array = arrayofsoze.array with random ...
        timer_starter = System.nanoTime();
        mergeSort(test.array)
        time_duration = System.nanoTime() - timer_start;
        print(time_duration)
        array_size * 2;
    }

    // insertion sort will always out-perform merge-sort



}